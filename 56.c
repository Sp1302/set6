# set6
#include<stdio.h>
int main()
{
char str;
printf("\nenter the string");
gtes(str);
if((str>a)&&(str<z)||(str>0)&&(str<9))
{
printf("\nit contains alphabet and numbers");
}
else
{
printf("\nno numbers and alphabets");
}
return 0;
}
